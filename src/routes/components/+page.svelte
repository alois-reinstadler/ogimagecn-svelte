<script lang="ts">import {base} from '$app/paths';import {componentIndex} from '$lib/site/nav.js';let query=$state('');const filtered=$derived(componentIndex.filter(x=>`${x.name} ${x.title} ${x.description}`.toLowerCase().includes(query.toLowerCase())));</script>
<svelte:head><title>Komponenten — ogimagecn-svelte</title><meta name="description" content="Alle Open-Graph-Bildkomponenten, gerendert mit der Produktions-Pipeline."/></svelte:head>
<div class="flex items-end justify-between gap-6"><div><p class="field-label text-muted-foreground">Katalog</p><h1 class="display mt-3 text-4xl sm:text-[2.75rem]">Komponenten</h1><p class="text-muted-foreground mt-4 max-w-2xl text-[17px]">{componentIndex.length} typisierte Ausgangspunkte für Commerce, Content, Releases, Events und Marken. Jede Seite zeigt echte Ausgabe, Props und Servercode.</p></div><span class="font-mono text-sm tabular-nums text-muted-foreground">{filtered.length}/{componentIndex.length}</span></div>
<label class="mt-8 flex max-w-md items-center rounded border bg-background px-3"><span class="sr-only">Komponenten filtern</span><input bind:value={query} type="search" placeholder="Komponenten filtern …" class="h-10 w-full bg-transparent outline-none"/></label>
<div class="mt-8 grid gap-5 md:grid-cols-2 xl:grid-cols-3">{#each filtered as item}<a href={`${base}/components/${item.name}/`} class="group overflow-hidden rounded-md border bg-card no-underline"><div class="bg-dots p-2"><img src={`${base}/previews/${item.name}.png`} width="600" height="315" alt={`${item.title} Vorschau`} loading="lazy" class="w-full rounded-sm"/></div><div class="border-t p-4"><h2 class="font-medium">{item.title}</h2><p class="text-muted-foreground mt-1 text-sm leading-6">{item.description}</p></div></a>{/each}</div>
