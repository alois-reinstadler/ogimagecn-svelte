<script lang="ts">import {base} from '$app/paths';import {guides} from '$lib/site/nav.js';</script>
<svelte:head><title>Documentation — ogimagecn-svelte</title><meta name="description" content="Guides for data-driven SVG and PNG output from Svelte."/></svelte:head>
<p class="field-label text-muted-foreground">Documentation</p><h1 class="display mt-3 text-4xl sm:text-[2.75rem]">Turn data into brand images</h1><p class="text-muted-foreground mt-4 max-w-3xl text-[17px] leading-relaxed">Connect product, content, or release data to typed Svelte 5 components. Satori lays them out deterministically; resvg creates PNG output when needed. The entire renderer stays server-side.</p><div class="mt-10 grid gap-4 md:grid-cols-2">{#each guides as guide}<a href={`${base}/docs/${guide.slug}/`} class="hover:bg-accent rounded-md border p-5 no-underline"><h2 class="display text-xl">{guide.title}</h2><p class="text-muted-foreground mt-2 text-sm leading-6">{guide.hint}</p></a>{/each}</div>
